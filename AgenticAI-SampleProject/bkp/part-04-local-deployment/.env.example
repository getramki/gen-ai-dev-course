# AWS Configuration
AWS_DEFAULT_REGION=us-east-1

# Note: AWS credentials are automatically loaded from:
# - Local: ~/.aws/credentials (mounted as volume)
# - Cloud: IAM roles when deployed in EKS/Agent Core